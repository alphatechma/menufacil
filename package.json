{
  "name": "menufacil",
  "version": "0.0.1",
  "private": true,
  "description": "MenuFacil - Plataforma multi-tenant de delivery de comida",
  "scripts": {
    "dev": "pnpm run dev:api & pnpm run dev:web & pnpm run dev:manager",
    "dev:api": "pnpm --filter api dev",
    "dev:web": "pnpm --filter web dev",
    "dev:manager": "pnpm --filter manager dev",
    "build:api": "pnpm --filter api build",
    "build:web": "pnpm --filter web build",
    "build:manager": "pnpm --filter manager build",
    "dev:waiter": "pnpm --filter @menufacil/waiter dev",
    "build:waiter": "pnpm --filter @menufacil/waiter build",
    "db:seed": "pnpm --filter api seed",
    "db:migration:generate": "pnpm --filter api migration:generate",
    "db:migration:run": "pnpm --filter api migration:run",
    "lint": "pnpm -r lint",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\""
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["@nestjs/core", "bcrypt", "esbuild"]
  }
}
