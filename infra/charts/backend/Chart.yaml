apiVersion: v2
name: backend
description: Chart do menufacil-api-java (Spring Boot 3.3) — deployment, service, ingress, configmap, secret, HPA, PDB.
type: application
version: 0.1.0
appVersion: "0.1.0"
