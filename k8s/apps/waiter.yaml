apiVersion: apps/v1
kind: Deployment
metadata:
  name: waiter
  namespace: menufacil
spec:
  replicas: 1
  selector:
    matchLabels:
      app: waiter
  template:
    metadata:
      labels:
        app: waiter
    spec:
      containers:
        - name: waiter
          image: menufacil/waiter:latest
          ports:
            - containerPort: 80
          env:
            - name: API_URL
              value: "http://api:3000"
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "100m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: waiter
  namespace: menufacil
spec:
  selector:
    app: waiter
  ports:
    - port: 80
      targetPort: 80
